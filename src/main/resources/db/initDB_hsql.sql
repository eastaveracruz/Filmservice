DROP TABLE user_roles IF EXISTS;
DROP TABLE films IF EXISTS;
DROP TABLE users IF EXISTS;
DROP SEQUENCE user_seq IF EXISTS;
DROP SEQUENCE film_seq IF EXISTS;

CREATE SEQUENCE user_seq
  AS INTEGER
  START WITH 1;

  CREATE SEQUENCE film_seq
  AS INTEGER
  START WITH 1;

CREATE TABLE users
(
  id               INTEGER GENERATED BY DEFAULT AS SEQUENCE user_seq PRIMARY KEY,
  login             VARCHAR(255)            NOT NULL,
  password         VARCHAR(255)
);

CREATE TABLE user_roles
(
  user_id INTEGER NOT NULL,
  role    VARCHAR(255),
  CONSTRAINT user_roles_idx UNIQUE (user_id, role),
  FOREIGN KEY (user_id) REFERENCES USERS (id) ON DELETE CASCADE
);

CREATE TABLE films
(
  id          INTEGER GENERATED BY DEFAULT AS SEQUENCE film_seq PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  image VARCHAR(255) NOT NULL,
  description VARCHAR(255) NOT NULL,
  genre VARCHAR(255) NOT NULL
);